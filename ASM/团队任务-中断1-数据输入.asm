#############################################################
#从外部读取数据 (SB TO,0(FROM))
#############################################################
.text
#---------保护现场---------
# 仅一级中断 无需保护现场
# 也无需开关中断
#---------获得数据---------
sb $t0, 0($0)
sb $t1, 0($1)
sb $t2, 0($2)
sb $t3, 0($3)
#---------恢复现场---------
# 仅一级中断 无需恢复现场
# 也无需开关中断
#---------返回---------
eret
